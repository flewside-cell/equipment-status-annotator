{
  "name": "equipment-status-annotator",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "predeploy": "npm run build",
    "deploy": "gh-pages -d dist -b gh-pages -r https://github.com/flewside-cell/equipment-status-annotator.git"
  },
  "devDependencies": {
    "gh-pages": "^6.1.1",
    "three": "^0.168.0",
    "vite": "^5.4.0"
  }
}
